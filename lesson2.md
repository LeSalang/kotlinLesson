# Урок 2: Функции, Boolean, if-else

### Повторение

На прошлом уроке мы изучили **переменные** в **Kotlin**. Давай вспомним, какие они бывают и как создаются.

Для создания переменных мы используем ключевые слова **val** и **var**.

Что обозначают **val** и **var**?

* **val** (*value*)

  Это ключевое слово, используемое для объявления неизменяемой переменной. Оно указывает, что переменной можно присвоить значение только один раз.
* **var** (*variable*)

  Это ключевое слово, используемое для объявления изменяемой переменной. Оно позволяет менять значение переменной после её создания.

```kotlin
val a: Int = 4 // нельзя изменить
val b: Int = 6 // нельзя изменить
var sum: Int = 0 // можно изменить после создания
sum = a + b // придаём новое значение после создания
```

Изучили некоторые **типы данных**:

| Тип данных | Описание              |                        Пример значений                         |
|:----------:|:----------------------|:--------------------------------------------------------------:|
|   **String**   | Строка текста         |                        "Hello, world!"                         |
|    **Byte**    | Маленькое целое число |                         от -128 до 127                         |
|   **Short**    | Короткое целое число  |                      от -32,768 до 32,767                      |
|    **Int**     | Целое число           |   от -2³¹ до 2³¹-1 <br/>(от -2 миллиардов до +2 миллиардов)    |
|    **Long**    | Длинное целое число   | от -2⁶³ до 2⁶³-1<br/>(от -9 квинтиллионов до +9 квинтиллионов) |

Узнали про **работу со строками**:

```kotlin
val name: String = "Биба"
val age: Byte = 4
println("Меня зовут $name. Через год мне будет ${age + 1} лет.") // Выведет в консоль "Меня зовут Биба. Через год мне будет 5 лет.
```
***
### Функции
Функция — это блок кода, который выполняет определённую задачу. 
Она помогает избежать дублирования кода и делает его более понятным.

Для создания функции в **Kotlin** необходимо использовать ключевое слово **fun**.

```kotlin
fun sum(number1: Int, number2: Int): Int {
    val sum: Int = number1 + number2 // основные вычисления функции
    return sum // здесь возвращается результат функции в то место, где функция вызывается
}
```
* **sum** - имя функции, пишется в **camelCase** _(это стиль написания, когда каждое новое слово пишется с заглавной буквы, а первое слово — с маленькой)_
* **(number1: Int, number2: Int)** - параметры функции
* **:Int** - тип возвращаемого значения
* **{...}** - тело функции
* **return** - ключевое слово, указывающее, что функция будет возвращать

Когда функция создана, её можно вызвать в другой части кода.
Для вызова функции необходимо указать её название и передать аргументы в круглые скобки. 

```kotlin
fun main() {
    val a: Int = 4
    val b: Int = 7
    val sum: Int = sum(number1 = a, number2 = b)
    println("Сумма чисел $a и $b равна $sum")
}

fun sum(number1: Int, number2: Int): Int {
    val sum: Int = number1 + number2
    return sum
}
```

Некоторые функции не возвращают никакого полезного значения, а просто выполняют какие-либо действия.
В таком случае после указания параметров функции мы пишем "__: Unit__" или же оставляем это место пустым.

```kotlin
fun printSumOfTwoNumbers(number1: Int, number2: Int) {
    val sum: Int = number1 + number2
    println("Сумма чисел $number1 и $number2 равна $sum") // данная функция не возвращает значение суммы, а выводит его в консоль в виде строки
}
```
***
### Boolean - логический тип данных
Логический тип данных (**Boolean**) используется в программировании для работы с двумя значениями:

* **true** (правда)
* **false** (ложь)

Эти значения отвечают на вопросы типа:

* "Верно ли это?"
* "Произошло ли это?"

```kotlin
var isFemale: Boolean = true // означает, что это женщина
isFemale = false // но вдруг она стала мужчиной 😯
```
***
### Конструкция if-else
Иногда в программировании необходимо выполнять определённый блок кода, только если верно определенное условие.
В таком случае для управления ходом исполнения программы удобно использовать конструкцию **if-else**.

Представим такую ситуацию, что мы хотим напечатать подруге, что мы пойдём загорать, только если на улице солнечно.
Мы должны передать компьютеру чёткую инструкцию _"**Если** солнечно, **то** печатай "Пойдём загорать""_.

На языке **Kotlin** это выглядит так:
```kotlin
val isSunny: Boolean = true
if (isSunny) { // после if в круглых скобках указывается какое-то условие, которое может быть только true или false
    println("Пойдём загорать") // в данном случае текст напечатается, т.к. isSunny = true, но если бы оно было false, но код бы не выполнился
}
```

Иногда мы хотим уточнить, что бы мы хотели делать, если условие не выполнится. 
В таком случае мы добавляем слово **else - иначе**.
В нашем примере это может быть так: _"**Если** солнечно, **то** печатай "Пойдём загорать", **иначе** печатай "Пойдём в кафе""_.

```kotlin
val isSunny: Boolean = true
if (isSunny) {
    println("Пойдём загорать")
} else {
    println("Пойдём в кафе")
}
```

Конечно, бывают и более сложные ситуации. На пример, у нас появляется ещё одно условие: "**Если** дождливо, **то** "Останемся дома"".
В таком случае добавляем конструкцию **иначе-если - else-if**:

```kotlin
val isSunny: Boolean = false // 🌞
val isRainy: Boolean = true // 🌧️
if (isSunny) { // Проверяем "солнечно ли?". В данном случае это не так, поэтому переходим на следующее условие.
    println("Пойдём загорать") // 🏖️
} else if (isRainy) { // Проверяем "дождливо ли?". В данном случае это правда, поэтому выполнится код в данных фигурных скобках
    println("Останемся дома") // 🏠
} else { // Код после else не будем выполнен, потому что верным оказалось выражение выше
    println("Пойдём в кафе") // 🍕
}
```
***
### Домашняя работа
В данной домашней работе будет необходимо создать несколько функции, которые будут вызывать внутри функции main().
* Функция, возвращающая квадрат переданного числа.
* Функция, принимающая длины сторон треугольника и возвращающая его периметр.
* Функция, принимающая имя собачки, породу, возраст и пол и печатающая в консоль что-то по типу "Продаётся Доберман Бусинка, девочка, 2 мес".
* Функции, принимающую число и печатающую в консоль чётное оно или нечётное.
* Функцию, принимающую число и возвращающую true, если число делится на 5 без остатка, или false, если число не делится на 5 без остатка.
* Функцию, принимающую трёхзначное число и возвращающую true, если оно делится на 3 без остатка, или false, если число не делится на 3 без остатка.
